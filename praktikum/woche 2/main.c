#include <stdio.h>
#include "acsii.h"
#include "multTable.h"
#include "dual.h"
#include "toUpperCase.h"

int main() {
  // ascii(10);

  // multTable();

  dual(10);

  // char c = ')';
  // char cUpper = toUpperCase(c);
  // printf("%c > %c\n", c, cUpper);

  return 0;
}